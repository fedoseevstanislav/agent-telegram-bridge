[project]
name = "claude-telegram-bridge"
version = "0.1.0"
description = "Telegram transport for local Claude Code and Codex sessions"
requires-python = "==3.12.*"
dependencies = []

[project.optional-dependencies]
dev = [
    "pip-audit>=2.9.0",
    "pytest==9.1.1",
]

[tool.uv]
package = false
required-version = "==0.9.26"

[tool.pytest.ini_options]
pythonpath = ["."]
